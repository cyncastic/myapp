%h1 Manufacturers
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @manufacturers.each do |manufacturer|
      %tr
        %td= link_to manufacturer.name, manufacturer
        %td.actions

          = link_to 'Edit', edit_manufacturer_path(manufacturer)
          = link_to 'Destroy', manufacturer, method: :delete, data: { confirm: 'Are you sure?' }

.actions= link_to 'New Manufacturer', new_manufacturer_path
